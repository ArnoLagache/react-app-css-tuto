import { AndroidOutlined } from "@ant-design/icons";
import React from "react";

const TableFilterComponent = ({ value }) => (
  <div>
    <AndroidOutlined />
    <p>{value}</p>
  </div>
);

export default TableFilterComponent;
